language: ruby

matrix:
  include:
    - sudo: required
      dist: precise
    - sudo: required
      dist: trusty
    - sudo: required
      dist: precise
      group: edge
    - sudo: required
      dist: trusty
      group: edge

services:
  - mongodb
  
install: true

script:
  - getent hosts localhost
  - mongo --eval "printjson(db.serverStatus())"
