sudo: required
dist: trusty
language: ruby
services:
  - docker
install:
  - cat /etc/apt/sources.list
  - travis_retry sudo apt-get update
  - sudo apt-get install docker-engine

script:
  - docker --version

