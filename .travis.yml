sudo: required
language: ruby
rvm:
  - jruby
services:
  - docker
  
env:
  - JRUBY_OPTS="--client -J-XX:+TieredCompilation -J-XX:TieredStopAtLevel=1 -J-Xss2m"

install: bundle install --jobs=4 --retry=3

script: true
