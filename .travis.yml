language: php

matrix:
  include:
    - sudo: required
      dist: precise
    - sudo: required
      dist: trusty

install: true
script:
  - php -S localhost:8000 > /dev/null &
  - sleep 5
  - curl localhost:8000
