language: python

python: 2.7

matrix:
  include:
    - sudo: required
      dist: precise
      env: SUDO=required DIST=precise
    - sudo: required
      dist: trusty
      env: SUDO=required DIST=trusty

install: true
script:
  - sudo apt-get install swig libffi-dev libxml2-dev libxslt-dev
