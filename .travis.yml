sudo: required
services:
  - docker
language: cpp
compiler: gcc
script:
  - gcc-5 --version
  - g++-5 --version
  - mysql --version
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5
