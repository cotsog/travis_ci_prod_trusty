sudo: required
services:
  - docker
before_install:
  - sudo cat /etc/default/docker
  - echo 'DOCKER_OPTS="-H tcp://127.0.0.1:2375 -H unix:///var/run/docker.sock -s devicemapper"' | sudo tee -a /etc/default/docker > /dev/null
  - sudo cat /etc/default/docker
  - service docker restart
