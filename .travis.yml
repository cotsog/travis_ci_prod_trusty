language: node_js

matrix:
  include:
  - os: linux
    dist: trusty
    sudo: required
  - os: linux
    dist: trusty
    sudo: required
    group: deprecated
    services: [mysql]

script:
  - sudo mysql -u root -e 'show databases'
  - sudo mysql -u root -e 'SELECT user FROM mysql.user'
