sudo: required
dist: precise

language: ruby

services:
  - mongodb

install: true
script:
  - (ps -edaf | grep mongo | grep -v grep)  || true
  - sudo /etc/init.d/mongodb status || true
  - sudo service mongodb status || true
  
