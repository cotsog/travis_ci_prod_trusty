sudo: required
dist: precise

language: ruby

services:
  - mongodb

install: true
script:
  - (ps -edaf | grep mongo | grep -v grep)  || true
  - /etc/init.d/mongod status || true
  - service mongod status || true
  
