sudo: required
services:
  - docker
env:
  - CLOUDSDK_CORE_DISABLE_PROMPTS=1
before_install:
  - gcloud version
  - curl https://sdk.cloud.google.com | bash
  - bash -l -c "./gcloud.sh components update kubectl"
