sudo: required

services:
  - docker

language: python
python:
  - "2.7"

install: true

script:
  - docker --version
  - docker-compose --version
  - pip install docker-compose
  - docker --version
  - docker-compose --version
