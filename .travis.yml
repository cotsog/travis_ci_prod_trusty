sudo: required
services:
  - docker
language: cpp
compiler: gcc
script:
  - gcc --version
  - g++ --version
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5
