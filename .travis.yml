# Trigger
language: go

sudo: required
dist: trusty

before_install:
  - rabbitmqctl status || true
  - rabbitmqctl stop || true
  - rabbitmqctl status || true
  - sudo rabbitmqctl stop || true
  - sudo rabbitmqctl status || true
  
install: true

script: true
