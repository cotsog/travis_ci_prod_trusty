language: ruby

dist: trusty

matrix:
  include:
    - sudo: required
    - sudo: false
    
services:
  - mysql
  
install: true
  
script:
  - mysql -V
  - mysql -e 'SHOW VARIABLES LIKE "%version%";'
