sudo: required
services:
  - docker
  - redis-server
  - postgresql

matrix:
  include:
    - env: POSTGRES_VESRION="default
    - env: POSTGRES_VESRION="9.3
      addons:
        postgresql: "9.3"

install: true

script:
  - docker info 
  - psql --version
  - redis-server --version
