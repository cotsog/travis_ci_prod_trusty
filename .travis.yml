sudo: required
dist: precise

language: ruby

services:
  - mongodb

install: true
script:
  - (ps -edaf | grep mongo | grep -v grep)  || true
  - /etc/init.d/mongodb status || true
  - service mongodb status || true
  
