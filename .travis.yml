sudo: required
services:
  - docker
install: true
script:
  - sudo apt-get update -qq
  - sudo apt-get -o Dpkg::Options::="--force-confdef" -o Dpkg::Options::="--force-confold" -y upgrade docker-engine
